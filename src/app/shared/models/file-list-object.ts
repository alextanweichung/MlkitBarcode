export interface FileListObject {
   filesId: number
   filesName: string
   filesType: string
   filesSize: number
   createdBy: string
   createdAt: Date
}

export interface ImageFileListModel {
   imageId: number
   additionalId: number
}